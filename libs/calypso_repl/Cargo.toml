[package]
name = "calypso_repl"
version = "0.1.0"
authors = ["ThePuzzlemaker <tpzker@thepuzzlemaker.info>"]
edition = "2018"
description = "REPL utilities for Calypso"
readme = "README.md"
homepage = "https://github.com/calypso-lang/calypso/tree/main/libs/calypso_repl"
repository = "https://github.com/calypso-lang/calypso/"
license = "MIT"

[dependencies]
rustyline = "7.1.0"
regex = "1.4.2"
# # Unfortunately, this causes a bug that doesn't have a released fix yet.
# # # Disable resolver feature as it includes some extra deps that are not needed.
# # # actix = { version = "0.10.0", default-features = false }
# actix = "0.10.0"
# actix-rt = "1.1"
# futures = "0.3.8"